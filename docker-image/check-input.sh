#!bin/bash
if [ -z "$REMOTE_HOST" ]
then
      echo "REMOTE_HOST is empty, exit"
      exit

elif [ -z "$REMOTE_USER" ]
then
      echo "REMOTE_USER is empty, exit"
      exit

elif [ -z "$REMOTE_USER" ]
then
      echo "REMOTE_USER is empty, exit"
      exit

elif [ -z "$REMOTE_PASSWORD" ]
then
      echo "REMOTE_PATH is empty, exit"
      exit
fi

echo "All fine, continue"